services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: aju
      POSTGRES_PASSWORD: aju_dev
      POSTGRES_DB: aju
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
